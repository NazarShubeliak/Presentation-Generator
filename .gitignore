# Python
.venv/
__pycache__/
*.pyc

# Generated / confidential content (see docs/00-time-log.md ground rules)
output/
reference/
# Raw slide exports reproduce confidential customer presentations (contact
# details, pricing/marketing content) — same rule as reference/, kept local only.
docs/slides/
# Extracted text/frame measurements reproduce the same confidential content
# verbatim (real strings, exact copy) — same rule as reference/.
docs/measurements/
# WP6 sanity-check scripts hardcode real Basel presentation copy (headlines,
# motif names, client list) to test the template — same rule as reference/.
src/sanity_check_build.py
src/sanity_check_extract_images.py
src/_wp8_prepare_test_images.py
# WP7 example data file: task brief requires real (not invented) presentation
# content here, which is exactly what the repo's confidentiality rule
# excludes — kept local only, same treatment as reference/.
data/example_01.json
# Ad-hoc test data files copied from example_01.json (same real text content).
data/test_*.json
# Real per-market photos referenced by data/example_01.json — same rule.
images/
# Real client/brand source assets (AR photobooth renders, market photos) —
# same confidentiality rule as reference/.
real_img/
# Throwaway blank-layout preview decks from src/_preview_layouts.py — not a
# deliverable.
templates/_preview_*.pptx
# WP9/WP10 validation doc references real Basel content/filenames — same
# confidentiality rule as reference/.
docs/06-comparison.md

# AI tooling — must never appear in this repository
.claude/
.claude*
CLAUDE.md
.mcp.json
.cursor/
.cursorrules
.windsurfrules
.github/copilot-instructions.md

# OS / editor noise
.vscode/
Thumbs.db
.DS_Store

# Licensed commercial fonts — not ours to redistribute
*.ttf
*.otf

# PowerPoint lock/autosave files
~$*.pptx
